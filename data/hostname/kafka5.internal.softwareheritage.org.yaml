---
# Use direct internet access for DNS
dns::forwarders: {}
dns::forwarder_insecure: false

backups::exclude:
  - srv/kafka

swh::apt_config::enable_non_free: true
packages:
  - amd64-microcode
  - zfs-dkms
