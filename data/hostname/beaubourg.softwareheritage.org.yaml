# needed for proxmox-ve
ssh::permitrootlogin: "yes"

icinga2::host::vars:
  load: high

dar::backup::exclude:
  - srv/containers

swh::apt_config::enable_non_free: true
packages:
  - intel-microcode

