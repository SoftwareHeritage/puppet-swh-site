smtp::mynetworks:
  - 127.0.0.0/8
  - "[::ffff:127.0.0.0]/104"
  - "[::1]/128"
  - 192.168.254.0/24  # storage array, for notification emails

# needed for proxmox-ve
ssh::permitrootlogin: "yes"

dar::backup::exclude:
  - srv/containers

icinga2::host::vars:
  load: high
