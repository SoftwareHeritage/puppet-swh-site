---
version: 4
datadir: data
hierarchy:
  - name: "private/hostname/%{trusted.certname}"
    backend: yaml
  - name: "private/location/%{::location}"
    backend: yaml
  - name: "private/defaults"
    backend: yaml
  - name: "hostname/%{trusted.certname}"
    backend: yaml
  - name: "location/%{::location}"
    backend: yaml
  - name: defaults_security
    backend: yaml
  - name: defaults
    backend: yaml
