---
:backends:
  - yaml
:yaml:
  :datadir: "%{::settings::environmentpath}/%{::environment}/data"
:hierarchy:
  - "private/hostname/%{::fqdn}"
  - "private/defaults"
  - "hostname/%{::fqdn}"
  - defaults
:merge_behavior: deeper
