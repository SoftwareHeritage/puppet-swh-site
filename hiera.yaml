---
version: 4
datadir: data
hierarchy:
  - name: "private/hostname/%{::fqdn}"
    backend: yaml
  - name: "private/location/%{::location}"
    backend: yaml
  - name: "private/defaults"
    backend: yaml
  - name: "hostname/%{::fqdn}"
    backend: yaml
  - name: "location/%{::location}"
    backend: yaml
  - name: defaults
    backend: yaml
