---
:backends:
  - yaml
  - module_data
:yaml:
  :datadir: "%{::settings::environmentpath}/%{::environment}/data"
:hierarchy:
  - "private/hostname/%{::certname}"
  - "private/defaults"
  - "hostname/%{::certname}"
  - defaults
:merge_behavior: deeper
