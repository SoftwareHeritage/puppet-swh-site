# Managed by puppet (class profile::systemd_journal::journalbeat), changes will be lost

[Service]
ReadOnlyDirectories=/
ReadWriteDirectories=-/var/lib/journalbeat
WorkingDirectory=/var/lib/journalbeat
