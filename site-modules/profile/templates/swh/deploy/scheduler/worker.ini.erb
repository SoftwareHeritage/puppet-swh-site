# Managed by puppet - modifications will be overwritten
# In defined class profile::swh::deploy::scheduler

[main]
task_broker = <%= @task_broker %>
task_modules = <%= @task_modules.join(', ') %>
task_queues =
