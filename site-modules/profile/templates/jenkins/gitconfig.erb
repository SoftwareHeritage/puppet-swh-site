# Git configuration for jenkins
# Handled by puppet (class profile::jenkins::base), manual changes will be lost

[user]
  name = Jenkins for Software Heritage
  email = jenkins@<%= @fqdn %>

[url "git@gitlab.softwareheritage.org:"]
	pushInsteadOf = https://gitlab.softwareheritage.org
