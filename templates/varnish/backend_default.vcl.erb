# backend_default.vcl
#
# Default backend definition.
#
# File managed by puppet. All modifications will be lost.

backend default {
    .host = "::1";
    .port = "<%= scope['::profile::varnish::backend_http_port'] %>";
}
